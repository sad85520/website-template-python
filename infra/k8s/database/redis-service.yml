# ClusterIP service；production.py 透過 REDIS_URL=redis://redis-service:6379/0 連線。
apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: web-template
spec:
  type: ClusterIP
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379
