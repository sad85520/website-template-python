# ========== Environment Variables ==========
.env
.env.local
src/frontend/.env.development
src/frontend/.env.production

# ========== Frontend ==========
src/frontend/node_modules/
src/frontend/dist/
src/frontend/.vite/
src/frontend/coverage/
src/frontend/*.local

# ========== Python ==========
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg
*.egg-info/
dist/
build/
.eggs/
*.whl
# `.mypy_cache/` 無前綴 `**/` 在 git 預設 pattern 下只匹配 repo 根目錄的條目；
# mypy 會在每個被檢查的 package 目錄下各生一份快取（例如 src/backend/.mypy_cache/），
# 若只寫 `.mypy_cache/` 會讓子目錄的快取仍被 `git status` 追蹤到。
# `**/` 明確覆蓋任意深度子目錄。同理套用到 ruff / pytest cache。
**/.mypy_cache/
**/.ruff_cache/
**/.pytest_cache/
.coverage
htmlcov/
.tox/
.venv/
venv/
src/backend/.venv/

# ========== Django ==========
src/backend/staticfiles/
src/backend/mediafiles/
src/backend/.env

# ========== Docker ==========
infra/nginx/ssl/*.pem
infra/nginx/ssl/*.crt
infra/nginx/ssl/*.key

# ========== K8S Secrets ==========
infra/k8s/secret.yml

# ========== OS ==========
.DS_Store
Thumbs.db

# ========== IDE ==========
.vscode/
.idea/
*.swp

# ========== Logs ==========
logs/
*.log
